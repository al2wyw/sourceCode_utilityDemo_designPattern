HP: (前端 后端 数据层)
后端:
1. 分布式缓存
2. 异步(消息队列)
3. 拆分(应用&数据)
4. 分布式集群扩展

HA: (客户端 -> 负载均衡LVS / 方向代理(nginx) -> web应用-> 分布式服务 -> 数据存储)
1. 复制 冗余
-- 主从复制
    -- 异步复制
    -- 同步复制
    -- 半同步复制
-- 多机房多地部署
    -- 同城双活 -> 两地三中心
    -- 异地双活 (数据sharding+避免跨地域事务，降低异地高延迟复制带来的高冲突，每个地域拥有全量数据，应用过滤掉异地消息) 难
    -- 异地多活 (数据sharding，数据本地域闭环，星状网络(中心节点作为枢纽与其他节点相互复制)，保证中心节点高可用) 难
    -- 单元化 指能支撑完成所有业务操作的服务和数据的自包含集合 (按照业务进行单元化切分(垂直)(如订单单元，流量按照用户id进行路由(横向))，应用框架改造，不是所有业务都能单元化) 超难
-- 故障转移模式
   双主(active-active)、主备(active-passive)、热备(hot-standby)、冷备 (tair支持双主多集群、主备单集群、独立多集群(特殊，只能作为缓存使用))
2. 故障转移
-- 负载均衡LVS / 方向代理(nginx) (keepalived(网络四层) + vip漂移 + 心跳检测)
-- web应用 (nginx的扩展功能: 存活检测+故障转移)
-- 分布式服务(服务框架的连接池)
-- 数据存储(redis,mysql等自身的高可用机制(sentinel、MHA)或使用keepalived)
3. 中间件故障转移实现:
-- 中心节点(zookeeper互相通信/namesrv不互相通信) : 侦测主节点状态并通知从节点
-- 从节点晋升 和 原主节点降级

HS:
1. 垂直扩展
-- 硬件能力提升
2. 水平扩展
-- 应用无状态