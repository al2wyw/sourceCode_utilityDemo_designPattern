HP: (前端 后端 数据层)
后端:
1. 分布式缓存
2. 异步(消息队列)
3. 拆分(应用&数据)
4. 分布式集群扩展

HA: (客户端 -> 负载均衡LVS / 方向代理(nginx) -> web应用-> 分布式服务 -> 数据存储)
1. 复制 冗余
-- 主从复制
    -- 异步复制
    -- 同步复制
    -- 半同步复制
-- 多机房多地部署
2. 故障转移
双主(active-active)/主备(active-passive) 热备/冷备
-- 负载均衡LVS / 方向代理(nginx) (keepalived)
-- web应用 (nginx扩展)
-- 分布式服务(服务框架)
-- 数据存储(redis,mysql高可用)
3. 中间件故障转移实现:
-- 中心节点(zookeeper互相通信/namesrv不互相通信) : 侦测主节点状态并通知从节点
-- 从节点晋升 和 原主节点降级

HS:
1. 垂直扩展
-- 硬件能力提升
2. 水平扩展
-- 应用无状态