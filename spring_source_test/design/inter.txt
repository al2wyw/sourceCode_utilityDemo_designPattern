秒杀方案
请求量少(流量削峰，答题过滤，分层缓存/校验过滤)，数据少(动态数据静态化，动静分离cdn)，依赖少(页面功能降级)，调用链路短(应用合并)，系统预热(数据预加载(数据库内部缓存),jvm手动gc)，无单点
难点: 扣减库存(读有缓存保护，写必须操作数据库)，热点资源隔离处理(单独数据库，避免影响其他业务)，减少并发量(请求串行排队)，提高吞吐量(库存分段加锁(分桶))

防止超卖和少卖

稳定性建设

数据一致性方案

分布式技术

中间件技术(选型)
mysql, redis(memcache), rocketmq(kafka)

java & jvm

网络tcp

架构设计 微服务 领域驱动

项目经验 方法论(可复用) 重做可提高点


github系统设计笔记:
缓存更新模式
背压和p999响应时间的关系
短链生成(时钟回拨)、推特消息推送触达(使用缓存来读写活跃客户的定量timeline)、爬虫和信息搜索(检测重复内容:基于网页内容生成签名的相似性算法)
大时间跨度的特征由小跨度特征聚合，有精度损失

分布式 DDIA
架构 软件架构基础
