秒杀方案
请求量少(流量削峰，答题过滤，分层缓存/校验过滤)，数据少(动态数据静态化，动静分离cdn)，依赖少(页面功能降级)，调用链路短(应用合并)， 无单点
难点: 扣减库存(读有缓存保护，写必须操作数据库)，热点资源隔离处理(单独数据库，避免影响其他业务)，减少并发量(请求串行排队)，提高吞吐量(库存分段加锁(分桶))

防止超卖和少卖

稳定性建设

数据一致性方案

分布式技术

中间件技术(选型)
mysql, redis(memcache), rocketmq(kafka)

java & jvm

网络tcp

架构设计 微服务 领域驱动

项目经验 方法论(可复用) 重做可提高点